# Dynamic Traefik configuration for platform services
# Tenant routes are configured via Docker labels when containers are created

http:
  routers:
    # Platform API
    api:
      rule: "Host(`api.localhost`)"
      entrypoints:
        - web
      service: api
      priority: 10

    # Platform Admin
    admin:
      rule: "Host(`admin.localhost`)"
      entrypoints:
        - web
      service: admin
      priority: 10

    # Platform Client (demo/testing)
    client:
      rule: "Host(`client.localhost`)"
      entrypoints:
        - web
      service: client
      priority: 10

    # Local dev tenant client - routes to host machine for hot reload
    dev-tenant:
      rule: "Host(`dev.localhost`)"
      entrypoints:
        - web
      service: dev-client-local
      priority: 10

    # Local dev tenant store API - routes to host machine for hot reload
    dev-tenant-store:
      rule: "Host(`store.dev.localhost`)"
      entrypoints:
        - web
      service: dev-store-local
      priority: 10

    # Catch-all for store subdomains (handled by Docker labels on tenant containers)
    store-catchall:
      rule: "HostRegexp(`store.{subdomain:[a-z0-9-]+}.localhost`)"
      entrypoints:
        - web
      service: noop@internal
      priority: 2

    # Catch-all for tenant subdomains (handled by Docker labels on tenant containers)
    tenant-catchall:
      rule: "HostRegexp(`{subdomain:[a-z0-9-]+}.localhost`)"
      entrypoints:
        - web
      service: noop@internal
      priority: 1

  services:
    # Platform API service
    api:
      loadBalancer:
        servers:
          - url: "http://econome-api:3001"

    # Platform Admin service
    admin:
      loadBalancer:
        servers:
          - url: "http://econome-admin:3000"

    # Platform Client service
    client:
      loadBalancer:
        servers:
          - url: "http://econome-client:3002"

    # Local dev client service - points to host machine (Next.js on port 3002)
    dev-client-local:
      loadBalancer:
        servers:
          - url: "http://host.docker.internal:3002"

    # Local dev store service - points to host machine (Medusa on port 9000)
    dev-store-local:
      loadBalancer:
        servers:
          - url: "http://host.docker.internal:9000"

  middlewares:
    # Rate limiting for API
    api-ratelimit:
      rateLimit:
        average: 100
        burst: 50

    # Security headers
    security-headers:
      headers:
        frameDeny: true
        browserXssFilter: true
        contentTypeNosniff: true
        forceSTSHeader: true
        stsSeconds: 31536000
        stsIncludeSubdomains: true

    # CORS for API
    api-cors:
      headers:
        accessControlAllowMethods:
          - GET
          - POST
          - PUT
          - PATCH
          - DELETE
          - OPTIONS
        accessControlAllowHeaders:
          - Content-Type
          - Authorization
        accessControlAllowOriginList:
          - "http://admin.localhost"
          - "http://localhost:3000"
        accessControlMaxAge: 100
        addVaryHeader: true
